// Dead component — discover/actions was removed with the producer pivot.
// Kept as empty export to avoid breaking any lingering references.
export default function InviteActions({ inviteId }: { inviteId: string }) {
  return null
}
