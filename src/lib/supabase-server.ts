/**
 * Supabase Server Client
 *
 * Stubbed for local development. Install @supabase/ssr and configure
 * env vars to activate.
 */

export function createServerSupabaseClient(): any {
  return null;
}

export function createServiceClient(): any {
  return null;
}
