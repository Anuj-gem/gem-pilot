// Default state for the dashboard's @modal parallel slot.
// Rendered whenever there's no active intercepting route — i.e. user is
// just looking at /dashboard with no report open.
//
// Anuj 2026-04-30 v0.7.

export default function ModalDefault() {
  return null
}
